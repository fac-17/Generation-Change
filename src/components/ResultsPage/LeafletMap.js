import React from "react";
import { Map, Marker, Popup, TileLayer, Tooltip } from 'react-leaflet';

const LeafletMap = () => {
  return <div className="container__map">
    <p>Leaflet Map</p>
  </div>;
};

export default LeafletMap;
